import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  card: {
    display: 'flex',
    textAlign: 'center',
  },
  cardDetails: {
    flex: 1,
  },
  cardMedia: {
    width: 145,
  },
});

export default useStyles;
